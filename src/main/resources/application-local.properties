# Local development overrides - NOT used in production
# Activate with: -Dspring.profiles.active=local (or IntelliJ run config)
vshield.h2-console.enabled=true
vshield.cookie-secure=false
spring.jpa.show-sql=true